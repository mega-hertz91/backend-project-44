const GAME_ROUND_COUNT = 3;
const MATH_SYMBOLS = ['*', '-', '*'];
const MATH_OPERATIONS = {
  '*': (a, b) => a * b,
  '-': (a, b) => a - b,
  '+': (a, b) => a + b,
};

export { GAME_ROUND_COUNT, MATH_SYMBOLS, MATH_OPERATIONS };
